version: 2
jobs:
    build:
        working_directory: ~/app
        parallelism: 1
        shell: /bin/bash --login
        docker:
        - image: simonmcc/circleci-ubuntu-trusty:latest
          command: /bin/cat
        steps:
        - checkout
        - run: uname -a
        - run: id
        - run: ls /
